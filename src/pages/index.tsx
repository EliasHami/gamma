import { type NextPage } from "next";

import PageLayout from "~/components/pageLayout";

const Home: NextPage = () => {
  return (
    <PageLayout noHeader>
      Gamma
    </PageLayout>
  );
};

export default Home;
